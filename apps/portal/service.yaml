apiVersion: v1
kind: Service
metadata:
  name: portal
  namespace: apps
  labels:
    app.kubernetes.io/name: portal
    app.kubernetes.io/part-of: ai-lab
    app.kubernetes.io/managed-by: argocd
spec:
  selector:
    app.kubernetes.io/name: portal
  ports:
    - name: http
      port: 80
      targetPort: http
